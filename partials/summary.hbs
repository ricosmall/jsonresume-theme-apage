{{#if resume.summary.length}}
<section id="summary" class="flex flex-wrap border-t border-dashed border-gray-500 mt-2 pt-2">
  <h2 class="text-lg font-bold w-full pb-2 md:w-16 md:pb-0 print:w-16 print:pb-0">自我<span class="md:block print:block">评价</span></h2>
  <ul class="highlights">
    {{#each resume.summary}}
    <li>{{.}}</li>
    {{/each}}
  </ul>
</section>
{{/if}}