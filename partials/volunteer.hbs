{{#if resume.volunteer.length}}
<section id="volunteer" class="flex flex-wrap border-t border-dashed border-gray-500 mt-2 pt-2">
	<h2 class="text-lg font-bold w-full pb-2 md:w-16 md:pb-0 print:w-16 print:pb-0">志愿<span
			class="md:block print:block">经历</span></h2>
	<div class="wrap">
		{{#each resume.volunteer}}
		<div class="font-bold">
			<div class="{{#unless @first}}mt-2{{/unless}}">
				<span class="inline-block w-full md:w-48 print:w-48">
					{{#if startDate}}
					{{startDate}}
					{{/if}}
					{{#if endDate}}
					- {{endDate}}
					{{else}}
					- 至今
					{{/if}}
				</span>
				{{#if organization}}
				<span class="inline-block w-full md:w-64 print:w-64">
					{{organization}}
				</span>
				{{/if}}
				{{#if position}}
				<span class="inline-block">
					{{position}}
				</span>
				{{/if}}
			</div>
			{{#if summary}}
			<div class="mt-1 text-gray-500">
				<span>{{summary}}</span>
				{{#if website}}
				<span class="ml-2">
					<a href="{{website}}">{{website}}</a>
				</span>
				{{/if}}
			</div>
			{{/if}}
			{{#if highlights.length}}
			<ul class="mt-1">
				{{#each highlights}}
				<li>- {{.}}</li>
				{{/each}}
			</ul>
			{{/if}}
		</div>
		{{/each}}
	</div>
</section>
{{/if}}